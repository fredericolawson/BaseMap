export default function Header() {
  return (
    <header className="text-center mb-12">
      <h1 className="title text-3xl font-bold mb-2">BaseMap</h1>
      <p className="text-gray-600 dark:text-gray-400">Airtable Schema Visualizer</p>
      <p className="text-xs text-gray-400 mt-4 max-w-lg mx-auto">
        Basemap does not collect, store, or process any personal information. All operations are performed directly in your browser, and no information is stored on the server.
      </p>
    </header>
  )
} 